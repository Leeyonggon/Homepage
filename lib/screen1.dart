import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class MScreen extends StatelessWidget {
  const MScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: const Text('첫 시작'),
      ),
    );
  }
}
